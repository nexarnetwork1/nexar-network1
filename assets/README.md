# Static assets organized by type.
# Public-facing assets remain in /public for Next.js static serving.
# Use this directory for source assets imported via bundler.

assets/
├── icons/       # SVG icons not yet in lucide
├── images/      # Source images (optimized copies go to /public)
└── fonts/       # Custom font files (if not using next/font)
