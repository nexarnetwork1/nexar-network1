-- Dev-only seed data. Do NOT run in production.
-- Requires existing auth.users rows created via Supabase Auth.

-- Example: after creating users in Supabase Auth dashboard, link profiles:
-- UPDATE public.profiles SET role = 'admin', profile_completed = true WHERE email = 'admin@nexarnetwork.org';
